# BetterC

This example shows how you can write a script to build a -betterC project.

# Building

from the root directory, after building DProjectManager as noted in the root directory's README run this command:

    DProjectManager/bin/DProjectManager Examples/BetterC/project.d

'Made with better C' should print to the console.
